package main

// Console app arguments
type Arguments map[string]*string

// Application class
type Application interface {
	// Set config struct
	SetConfig (cfg interface{}) *Application
	// Get config struct
	GetConfig (cfg interface{}) interface{}
	// Start application
	Start (arguments Arguments) *Application
}
